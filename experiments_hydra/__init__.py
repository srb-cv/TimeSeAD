"""
Hydra + MLflow experiment entrypoints for TimeSeAD.

This package is intentionally scoped as a pilot migration layer that mirrors a
small subset of ``timesead_experiments`` while keeping the core model, data,
and trainer code unchanged.
"""

